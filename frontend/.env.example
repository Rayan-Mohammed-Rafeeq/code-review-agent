# Copy to `.env` for local development.
# Do NOT commit `.env` (even if values look "public", it tends to accrete secrets over time).

# Base URL the frontend should call.
# - Local dev: Vite proxy / relative path is fine.
# - Deployed: set to your API origin (e.g. https://my-api.example.com)
VITE_API_BASE=/api

# Example public (non-secret) values.
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
PING_MESSAGE="ping pong"

