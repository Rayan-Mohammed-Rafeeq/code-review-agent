services:
  - type: web
    name: code-review-agent-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: CODE_REVIEW_CORS_ORIGINS
        value: https://code-review-agent.streamlit.app